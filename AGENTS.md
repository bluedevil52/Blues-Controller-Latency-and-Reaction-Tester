# Project ground rule

The user has explicitly instructed: never delete anything on their PC.
Do not delete files or directories or run cleanup commands.
The user subsequently requested a trash icon for individual leaderboard results.
This is implemented as moving the selected JSON file into results/trash, with Undo.
That scoped UI action is authorized; unrelated deletion or cleanup is not.
Preserve existing work. Make backups before replacing existing substantial content.
